# API-only additions. The parent project's requirements.txt is still required
# (fastapi, uvicorn, numpy, faiss-cpu, ...). Install it first, then this file.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
